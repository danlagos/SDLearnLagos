

describe("capitalize",() => {
  test("takes a string in lower case, and returns upper case", () => {
    expect(energy("tired")).toBe("TIRED")

var capitalize = (capObj) => {
  var upper = capObj.toUpperCase()
  return upper
}
